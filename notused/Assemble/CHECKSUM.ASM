DATA SEGMENT
  SUM DW ?
DATA ENDS

CODE SEGMENT
  ASSUME CS:CODE
START:
  MOV BX,40H
  MOV DS,BX
  MOV CX,1024
  MOV SI,0
  XOR AX,AX

AGAIN:
  ADD AX,[SI]
  INC SI
  INC SI
  LOOP AGAIN

  ASSUME DS:DATA
  MOV BX,DATA
  MOV DS,BX
  MOV SUM,AX
  MOV AH,4CH
  INT 21H
CODE ENDS
  END START