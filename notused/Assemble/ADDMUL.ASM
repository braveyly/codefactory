DATA SEGMENT
  XXX DB 123
  YYY DB 101
  ZZZ DW ?
DATA ENDS

CODE SEGMENT
  ASSUME DS:DATA,CS:CODE
START:
  MOV AX,DATA
  MOV DS,AX
  MOV AL,XXX
  XOR AH,AH
  MOV CL,YYY
  XOR CH,CH
  XOR DX,DX
  JCXZ LAB

MUL1:ADD DX,AX
     LOOP MUL1

LAB:MOV ZZZ,DX
    MOV AH,4CH
    INT 21H
CODE ENDS
  END START